import{useEffect,useState}from'react';import{CreditCard,LayoutDashboard,PiggyBank,Search,Settings,Sparkles,WalletCards}from'lucide-react';import{AnimatePresence,motion}from'motion/react';import{useNavigate}from'react-router-dom';import{useAppStore}from'../store/use-app-store';const items=[['Overview','Net worth and cash flow','/',LayoutDashboard],['Accounts','Connected financial accounts','/accounts',WalletCards],['Transactions','Search your transaction history','/transactions',CreditCard],['Budgets','Track monthly spending plans','/budgets',PiggyBank],['Settings','Manage preferences','/settings',Settings]]as const;export function CommandPalette(){const open=useAppStore(s=>s.palette),setOpen=useAppStore(s=>s.setPalette),nav=useNavigate(),[q,setQ]=useState('');useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key==='k'){e.preventDefault();setOpen(!open)}if(e.key==='Escape')setOpen(false)};addEventListener('keydown',fn);return()=>removeEventListener('keydown',fn)},[open,setOpen]);return <AnimatePresence>{open&&<div className="fixed inset-0 z-50 grid place-items-start bg-black/25 px-4 pt-[15vh] backdrop-blur-sm" onMouseDown={()=>setOpen(false)}><motion.div initial={{opacity:0,scale:.98,y:-8}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:.98}} onMouseDown={e=>e.stopPropagation()} className="w-full max-w-xl overflow-hidden rounded-xl border border-line bg-panel text-ink shadow-float"><div className="flex items-center gap-3 border-b border-line px-4"><Search size={17} className="text-muted"/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search Asteris…" className="h-14 flex-1 bg-transparent text-sm outline-none"/><kbd className="rounded border border-line px-1.5 py-0.5 text-[10px] text-muted">ESC</kbd></div><div className="p-2"><p className="px-2 py-2 text-[10px] font-semibold uppercase tracking-widest text-muted">Navigate</p>{items.filter(i=>i[0].toLowerCase().includes(q.toLowerCase())).map(([name,desc,to,Icon])=><button key={name} onClick={()=>{nav(to);setOpen(false)}} className="flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-left hover:bg-muted/10"><span className="grid h-8 w-8 place-items-center rounded-lg border border-line"><Icon size={15}/></span><span><b className="block text-xs font-medium">{name}</b><small className="text-[11px] text-muted">{desc}</small></span><span className="ml-auto text-[10px] text-muted">Go →</span></button>)}</div><div className="flex items-center gap-2 border-t border-line px-4 py-3 text-[10px] text-muted"><Sparkles size={12} className="text-violet-500"/>Powered by Asteris intelligence</div></motion.div></div>}</AnimatePresence>}
