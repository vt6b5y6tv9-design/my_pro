import{useEffect,useState}from'react';import{CreditCard,LayoutDashboard,PiggyBank,Search,Settings,WalletCards}from'lucide-react';import{AnimatePresence,motion}from'motion/react';import{useNavigate}from'react-router-dom';import{useAppStore}from'../store/use-app-store';const items=[['总览','净资产与现金流','/',LayoutDashboard],['账户','管理已连接账户','/accounts',WalletCards],['交易流水','搜索和分类交易','/transactions',CreditCard],['预算与目标','管理支出计划','/budgets',PiggyBank],['设置','管理个人偏好','/settings',Settings]]as const;export function CommandPalette(){const open=useAppStore(s=>s.palette),setOpen=useAppStore(s=>s.setPalette),go=useNavigate(),[q,setQ]=useState('');useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key==='k'){e.preventDefault();setOpen(!open)}if(e.key==='Escape')setOpen(false)};addEventListener('keydown',fn);return()=>removeEventListener('keydown',fn)},[open,setOpen]);return <AnimatePresence>{open&&<div className="fixed inset-0 z-50 grid place-items-start bg-black/25 px-4 pt-[15vh] backdrop-blur-sm" onMouseDown={()=>setOpen(false)}><motion.div initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} onMouseDown={e=>e.stopPropagation()} className="w-full max-w-xl rounded-xl border border-line bg-panel p-2 text-ink shadow-float"><div className="flex items-center gap-3 border-b border-line px-3"><Search size={16}/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索 Asteris…" className="h-12 flex-1 bg-transparent text-sm outline-none"/></div>{items.filter(i=>i[0].includes(q)).map(([name,desc,to,Icon])=><button key={name} onClick={()=>{go(to);setOpen(false)}} className="flex w-full items-center gap-3 rounded-lg px-3 py-3 text-left hover:bg-muted/10"><Icon size={15}/><span><b className="block text-xs">{name}</b><small className="text-[10px] text-muted">{desc}</small></span><span className="ml-auto text-[10px] text-muted">前往 →</span></button>)}</motion.div></div>}</AnimatePresence>}
