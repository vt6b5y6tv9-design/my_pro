import{Bell,Check,Moon,Shield,Sun,UserRound}from'lucide-react';import{useAppStore}from'../../store/use-app-store';import{Button}from'../../components/ui';export function Settings(){const{theme,toggleTheme}=useAppStore();return <div className="mx-auto max-w-4xl p-5 lg:p-8"><h2 className="text-2xl font-semibold">Settings</h2><p className="mt-1 text-xs text-muted">Configure your personal workspace experience.</p><div className="mt-8 grid gap-8 md:grid-cols-[180px_1fr]"><nav className="space-y-1 text-xs"><button className="flex w-full items-center gap-2 rounded-lg bg-muted/10 px-3 py-2 font-medium"><UserRound size={14}/>General</button>{[[Bell,'Notifications'],[Shield,'Security']].map(([Icon,label])=><button key={String(label)} className="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-muted hover:bg-muted/5">{typeof Icon!=='string'&&<Icon size={14}/>} {label as string}</button>)}</nav><section><div className="border-b border-line pb-6"><h3 className="text-sm font-semibold">Profile</h3><p className="mt-1 text-xs text-muted">This is how others see you in Asteris.</p><div className="mt-5 grid gap-4 sm:grid-cols-2">{[['Full name','Jordan Davis'],['Email','jordan@asteris.so']].map(([a,b])=><label key={a} className="text-xs">{a}<input defaultValue={b} className="mt-2 h-9 w-full rounded-lg border border-line bg-panel px-3 outline-none"/></label>)}</div></div><div className="border-b border-line py-6"><h3 className="text-sm font-semibold">Appearance</h3><p className="mt-1 text-xs text-muted">Choose the interface that works best for you.</p><div className="mt-5 grid grid-cols-2 gap-3"><button onClick={()=>theme==='dark'&&toggleTheme()} className={`relative rounded-lg border p-3 text-left ${theme==='light'?'border-ink':'border-line'}`}><Sun size={15}/><p className="mt-5 text-xs font-medium">Light</p>{theme==='light'&&<Check className="absolute right-3 top-3" size={14}/>}</button><button onClick={()=>theme==='light'&&toggleTheme()} className={`relative rounded-lg border bg-zinc-950 p-3 text-left text-white ${theme==='dark'?'border-violet-500':'border-line'}`}><Moon size={15}/><p className="mt-5 text-xs font-medium">Dark</p>{theme==='dark'&&<Check className="absolute right-3 top-3" size={14}/>}</button></div></div><div className="flex justify-end pt-6"><Button className="bg-ink text-canvas">Save changes</Button></div></section></div></div>}
